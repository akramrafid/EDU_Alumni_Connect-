import 'package:flutter/material.dart';

class AlumniDetailScreen extends StatelessWidget {
  const AlumniDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('AlumniDetailScreen'),
      ),
      body: Center(
        child: Text('AlumniDetailScreen Placeholder'),
      ),
    );
  }
}
