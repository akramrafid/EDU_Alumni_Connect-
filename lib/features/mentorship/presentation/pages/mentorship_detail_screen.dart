import 'package:flutter/material.dart';

class MentorshipDetailScreen extends StatelessWidget {
  const MentorshipDetailScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('MentorshipDetailScreen'),
      ),
      body: Center(
        child: Text('MentorshipDetailScreen Placeholder'),
      ),
    );
  }
}
